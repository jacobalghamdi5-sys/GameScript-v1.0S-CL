# GameScript Developer Core Manual

## Welcome to the Core Engine Workspace
This guide establishes setup requirements for local build evaluation of the engine matrix.

## Environment Setup
1. Clone the workspace: `git clone https://github.com/jacobalghamdi5-sys/GameScript-v1.0S-CL.git`
2. Run standard initialization checks to verify `.astro` structures.

## System Workflow Directives
- **Rule 1**: Never hardcode state mutations outside conditional interceptors (`<if>`).
- **Rule 2**: Retain pixel-exact font scales for Google Sans and Google Sans Flex elements across layouts.
